% ============================================================================
% CV5 RESULTS — NATIVE LaTeX tables under the unified 5-fold cross-validation
% protocol. The three \input fragments (cv5_{main,scaling,ablation}_table.tex) are
% regenerated from results_cv5/ by build_cv5_tex.py on every refresh cycle, and the
% paper is recompiled, so cells fill in real time as folds finish ("run..." = pending).
% They SUPERSEDE the pre-CV5 tables archived in archive/paper_pre_cv5_2026-07-13/.
% ============================================================================
\begin{table*}[t]
\centering
\IfFileExists{cv5_main_table.tex}{\input{cv5_main_table.tex}}{\emph{(main-results table renders here as 5-fold CV runs land)}}
\caption{\textbf{Main results: the efficiency ladder (5-fold CV).} Macro-F1 mean$\pm$SD; the
\colorbox{bestgreen}{best per column} is highlighted; Avg is over all $14$ datasets (the three pan-cancer cohorts
PM/PC/3M are included on the same footing as every other column). The learned graph supplies
the accuracy gain at $4\times$ fewer parameters.}
\label{tab:main_cv5}\label{tab:learned}\label{tab:ladder}
\end{table*}

% Injection-site ablation (our main contribution: WHERE biology should enter) -- now a
% single figure spanning all four cohorts (this MERGES and REPLACES the former Table 3).
\begin{figure*}[t]
\centering
\IfFileExists{figs/biorouter_bars.pdf}{\includegraphics[width=0.82\textwidth]{figs/biorouter_bars.pdf}}{\emph{(injection-site ablation bar chart renders here)}}
\caption{\textbf{Where should biology enter? (main contribution).} Injection-site ablation
(macro-F1, 5-fold CV): the learned interaction graph injected at no site, the router only
(zero-init graph-conv), the embedding only, or \emph{both}, on all four ablation cohorts ---
Baron, Muraro (single-cell, co-expression graph) and PM, 3M (pan-cancer multi-omics, provided
Reactome graph). Injecting at \emph{both} sites (bioMoR) is the winning site on every cohort;
each bar equals the corresponding cell of the main results (Table~\ref{tab:main_cv5}).}
\label{fig:injection}
\end{figure*}

\begin{figure*}[t]
\centering
\IfFileExists{figs/baron_loss.pdf}{\includegraphics[width=0.42\textwidth]{figs/baron_loss.pdf}}{}%
\hfill
\IfFileExists{figs/baron_val_f1.pdf}{\includegraphics[width=0.42\textwidth]{figs/baron_val_f1.pdf}}{}%
\caption{\textbf{Training dynamics on Baron (5-fold CV).} Training loss (left) and
validation macro-F1 (right) vs.\ \emph{estimated cloud cost} (USD; A100 @ \$1.80/hr,
i.e.\ measured A100 GPU-seconds priced at an on-demand cloud rate), for all four
architectures (Vanilla, Recursive, MoR, bioMoR). Lines are the mean over the same 5 folds
as Table~\ref{tab:main_cv5}; shaded bands are $\pm1$ SD across folds.}
\label{fig:baron_curves}
\end{figure*}

\begin{figure}[t]
\centering
\IfFileExists{figs/pareto_efficiency.pdf}{\includegraphics[width=\columnwidth]{figs/pareto_efficiency.pdf}}{\emph{(accuracy-vs-compute Pareto figure renders here as runs land)}}
\caption{\textbf{Accuracy--compute trade-off on Baron.} Macro-F1 vs.\ relative FLOPs
(norm.\ to Vanilla); marker size $\propto$ parameters. bioMoR is on the Pareto frontier;
the Vanilla stack ($4\times$ parameters, full compute) is dominated.}
\label{fig:pareto}
\end{figure}

% Model-size scaling table (tab:scaling / fig:scaling_cv5) moved to the supplementary.
% Component-ablations table (tab:ablation_cv5 / tab:confound) removed from the paper.
